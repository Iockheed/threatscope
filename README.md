# ThreatScope — Python Dependencies

Install dependencies:
pip install -r requirements.txt

# Requirements:

pip install python-whois   # WHOIS lookups
pip install dnspython      # DNS queries
pip install requests       # HTTP requests / APIs
pip install yara-python    # YARA malware rules engine
pip install oletools       # Office document macro analysis
pip install pefile         # PE (Windows executable) analysis
